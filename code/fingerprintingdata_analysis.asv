clc;clear;close all;
%----------- load -----------%
load('a_radiomapdata');
apqty = 3;
%--------- Êý¾Ý·ÖÎö ----------%
for i = 1:apqty
    temp = cellfun(@(x) x(1), a_radiomapdata, 'UniformOutput', false);
    for j = 1:size(a_radiomapdata,1)
        y = cell2mat(temp(j,:));
        x = 1:size(y, 2);
        subplot(2,2,j); plot(x,y);title('RSSI');
    end
    figure(i);
end